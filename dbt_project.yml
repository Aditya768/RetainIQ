name: 'retainiq'
version: '1.0.0'
profile: 'retainiq'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  retainiq:
    staging:
      +materialized: view
    marts:
      +materialized: view
